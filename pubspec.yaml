name: qabool_app
description: A new Flutter matrimonial project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.0
  flutter_svg: ^2.0.9
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  socket_io_client: ^3.0.1
  provider: ^6.1.1
  intl: ^0.19.0
  image_picker: ^1.0.7
  timeago: ^3.6.1
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
